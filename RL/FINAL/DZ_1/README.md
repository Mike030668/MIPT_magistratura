# Многорукие бандиты

[ОТЧЕТ wandb](https://wandb.ai/mike-puzitskiy/multi-armed-bandit/reports/-regret---Vmlldzo3OTQwOTMw)  "Кривые потерь (regret) для каждого из алгоритмов многоруких бандитов"

# Контекстные бандиты

## Пакетные модели (описание)

**Логистическая регрессия и контекстные бандиты:**

- `Bootstrapped Upper Confidence Bound` (C.I.=80%) - Использует метод бутстрапа для оценки доверительных интервалов и выбора действий.
- `Bootstrapped Thompson Sampling` - Бутстрапирование для метода Thompson Sampling.
- `Separate Classifiers + Beta Prior` - Отдельные классификаторы для каждого действия с априорным распределением Бета.
- `Epsilon-Greedy` (p0=20%, decay=0.9999) - Жадный алгоритм с вероятностью случайного выбора, которая постепенно уменьшается.
- `Logistic Upper Confidence Bound` (C.I.=70%) - Логистическая регрессия с оценкой доверительных интервалов.
- `Adaptive Greedy (decaying threshold)` - Адаптивная жадная стратегия с уменьшающимся порогом.
- `Adaptive Greedy (p0=30%, decaying percentile)`- Адаптивная жадная стратегия с уменьшающимся перцентилем.
- `Explore First (n=1,500)` - Сначала исследует действия, затем использует полученные знания.
- `Active Explorer` - Активное обучение для выбора действий.
- `Softmax Explorer` - Использует softmax-функцию для выбора действий.
- `Adaptive Active Greedy` - Адаптивная активная жадная стратегия.
- `Overall Best Arm (no context)` - Лучшее действие без учета контекста (контрольная линия).

## Описание датасета BibTeX

Датасет BibTeX Dataset предствлен на [сайте ](http://manikvarma.org/downloads/XC/XMLRepository.html)

- данные в датасете рассматривают различные задачи по категоризации веб-страниц, рекомендации связанных веб-страниц и рекомендации товаров. Они включают в себя мультимодальные наборы данных и наборы данных, в которых метки имеют текстовые характеристики.

- Многомерные признаки (контекст):
  - Каждый документ представлен вектором признаков, что позволяет использовать эти признаки как контекст для контекстных бандитов.

  - Признаки могут включать такие данные, как слова или темы, которые описывают документ, что является типичным контекстом для контекстных бандитов.

- Многомерные метки (действия):
  - Каждая метка представляет собой тег, связанный с документом.
  - В задаче контекстных бандитов действиями могут быть различные теги, которые агент должен выбрать на основе контекста (признаков документа).


- Задачи и цели при использовании BibTeX Dataset
  - Задача: На основе признаков документа (контекста) выбрать наилучшие теги (действия) для этого документа, чтобы максимизировать общее вознаграждение (например, соответствие тегов).
  - Цель: Построить и протестировать алгоритмы контекстных бандитов, чтобы определить, какой алгоритм лучше всего подходит для максимизации вознаграждения в данной задаче.


**Сравнительный график тестов на BibTeX**
<img src="images/Comparison of Online Contextual Bandit Policies  BibTeX.png" alt="png"  width="800"/> 

## Подробный анализ графика

**Bootstrapped Upper Confidence Bound:**
- Показывает высокие кумулятивные вознаграждения и стабильный рост на протяжении всего эксперимента.
- Один из лучших алгоритмов на этом датасете.

---

**Bootstrapped Thompson Sampling:**

- Тоже показывает хороший рост, но немного уступает Bootstrapped UCB.
- Более стабилен на начальных этапах по сравнению с другими методами.

---
**Separate Classifiers + Beta Prior:**

- Хорошо себя показывает, особенно на средних и поздних этапах.
- Демонстрирует эффективность использования отдельных классификаторов для каждого действия.

---
**Epsilon-Greedy:**
- Имеет стабильный, но медленный рост.
- Вероятно, не оптимален для данного датасета, так как слишком часто выбирает случайные действия.

---
**Logistic Upper Confidence Bound:**

- Показал один из лучших результатов, стабильно высокий рост.
- Подтверждает эффективность использования UCB с логистической регрессией.

---
**Adaptive Greedy (decaying threshold) и Adaptive Greedy (decaying percentile):**

- Оба показали хорошие результаты, особенно на поздних этапах.
- Адаптивные стратегии хорошо справляются с изменяющимися условиями.

---
**Explore First:**

- Стратегия исследования на начальных этапах дала хорошие результаты.
- После 1500 итераций перешел на использование, показывая стабильный рост.

---
**Active Explorer:**

- Показал средние результаты, стабилен, но не лучший.
- Вероятно, не полностью использует возможности активного обучения на этом датасете.

---
**Softmax Explorer:**
- Показал одни из лучших результатов.
- Softmax хорошо справляется с выбором действий на основе экспоненциальных оценок.

---
**Adaptive Active Greedy:**
- Показал хорошие результаты, особенно на поздних этапах.
- Комбинация адаптивных стратегий и активного обучения оказалась эффективной.

---
**Overall Best Arm (no context):**

- Контрольная линия показывает, что учитывание контекста значительно улучшает результаты.
- Лучшая рука без контекста не может конкурировать с алгоритмами, учитывающими контекст.

---
---

## Выводы по результатам экспериментов

*Лучшие алгоритмы:*

- `Bootstrapped Upper Confidence Bound (C.I.=80%)` и `Logistic Upper Confidence Bound (C.I.=70%)` показали самые высокие результаты.
- `Softmax Explorer` и `Separate Classifiers + Beta Prior` -  также показали высокие результаты.

*Средние алгоритмы:*

- `Adaptive Greedy` (оба варианта) и `Adaptive Active Greedy ` - показали хорошие результаты, особенно на поздних этапах.
- `Explore First` -  показал хорошие результаты после начального этапа исследования.

*Менее успешные алгоритмы:*

- `Bootstrapped Thompson Sampling` -  оказался менее эффективным и показал результаты ниже, чем Overall Best Arm (no context).
- `Epsilon-Greedy` - показал стабильный, но не лучший результат.

**Причины и рекомендации**
- `Bootstrapped Thompson Sampling`:
  - Возможно, метод бутстрапа для Thompson Sampling не очень хорошо подходит для данного датасета.
  - Стоит рассмотреть настройку гиперпараметров или использовать альтернативные методы.

- `Epsilon-Greedy`:
  - Высокая начальная вероятность случайного выбора может снижать эффективность.
  - Можно попробовать уменьшить начальное значение 
  𝜖 или изменить скорость затухания.

**Рекомендации:**
- Использовать `Bootstrapped UCB` и `Logistic UCB` для задач, где важна высокая эффективность, а `Adaptive Greedy` и `Softmax Explorer` для сценариев с изменяющимися условиями.
